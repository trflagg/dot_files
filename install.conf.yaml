- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.spacemacs: spacemacs
    ~/.tmux.conf: tmux.conf
    ~/.vimrc: vimrc
    ~/.vim: vim
    ~/.Xmodmap: xmodmap

- shell:
  - [git submodule update --init --recursive, Installing submodules]
